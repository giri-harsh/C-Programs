#include <stdio.h>
int main(){
    int n=5;
    int arr[n];
    int key;
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&key);
    int flag=0;
    for (int i=0;i<n;i++){
        if(arr[i]==key){
            printf("element found at %d or %d",arr[i],i);
            flag=1;
            break;
        }
        
    }
    if (flag==0)
    printf("\n Element not found");

 

}
